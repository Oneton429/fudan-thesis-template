= 使用方法

== 基础信息配置

模板的核心信息高度集成在 `config.typ` 文件中。用户在开始撰写前，需更新以下关键字段以确保封面与页眉信息的准确性：

`title`：论文的正式题目。

`author`：作者姓名。

`student_id`：学号（用于封面填充）。

`school` 与 `major`：所属学院与所学专业。

`instructor`：指导教师姓名及其职称。

== 章节管理与撰写

本模板采用了模块化的结构，建议将论文的主体内容置于 `chapters/` 目录下进行维护：

新建章节：在 `chapters/` 文件夹下创建 `.typ` 文件（例如 `03.typ`）。

内容组织：在文件中直接使用 `=`、`==` 等符号定义标题层级。模板会自动应用复旦规范的标题样式（如“第一章”风格）。

参考文献：将 BibLaTeX 格式的条目添加至 `reference.bib`。在正文中通过 `@key` 语法进行引用，模板将自动在末尾生成参考文献列表。
== 编译与预览

推荐使用 Typst 命令行工具或跨平台的 VS Code 插件进行文档构建：

实时监控：运行 `typst watch main.typ`。在此模式下，每当您保存源文件，预览 PDF 都会在毫秒级内完成更新。
正式构建：完成写作后，运行 `typst compile main.typ` 即可生成最终用于提交的 main.pdf 格式文件。
项目结构：请确保 `styles.typ`、`utils.typ` 与 `main.typ` 处于同一目录下，以保证样式逻辑的正确导入。
